node_modules
.next
out
.env
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.design-skills

# Local JSON store and admin uploads (runtime data, not source)
.data
public/uploads

# MCP config holds an API key — never commit it
.kiro/settings/mcp.json
**/mcp.json
